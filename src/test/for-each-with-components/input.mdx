import MessageItem from './message-item.mdx';

{/**
  * @typedef MessageItem
  * @property {"user" | "assistant"} role
  * @property {string} message
  */}
{/**
  * @typedef Props
  * @property {Array<MessageItem>} messageHistory
  */}

# Main

<ForEach arr={props.messageHistory}>
  {(item) => (
    <MessageItem item={item} />
  )}
</ForEach>
# Main

## List Index Items

<ForEach arr={props.arr}>
  {(item) => (
    <>
      ### Component {index}
    </>
  )}
</ForEach>

## List array items

<ForEach arr={props.nestedArr}>
  {(item) => (
    <>
      * List Item {item.num} {item.deep.name}
    </>
  )}
</ForEach>

## Nested Map

<ForEach arr={props.arr}>
  {(outerItem) => (
    <>
      ### Outer Loop {outerItem}
      <ForEach arr={props.nestedArr} as="innerItem">
        {(innerItem) => (
          <>
            * Inner Loop: {innerItem.num}
          </>
        )}
      </ForEach>
    </>
  )}
</ForEach>
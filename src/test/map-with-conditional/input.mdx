# Main

<Map arr={props.messageHistory}>
  <Conditional>
    <If condition={item.role == 'user'}>
      <User>{item.message}</User>
    </If>
    <ElseIf condition={item.role == 'assistant'}>
      <Assistant>{item.message}</Assistant>
    </ElseIf>
  </Conditional>
</Map>
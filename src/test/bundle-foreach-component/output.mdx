<ForEach arr={props.messageHistory}>
  {(item) => (

    <>
      ## Message: {props.msg}
    </>

    )}
</ForEach>

<ForEach arr={props.messageHistory}>
  {(item) => (

    <>
      ## Message: {props.msg}

      ## Message: {item}
    </>

    )}
</ForEach>
